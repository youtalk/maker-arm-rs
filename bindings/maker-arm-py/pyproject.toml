[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "maker-arm-rs"
version = "0.1.0"
description = "Python bindings for the maker-arm-rs Rust SDK (module: maker_arm_rs)"
requires-python = ">=3.10"
license = "Apache-2.0"

[tool.maturin]
module-name = "maker_arm_rs"
